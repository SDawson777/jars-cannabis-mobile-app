export interface FAQItem {
  id: string;
  question: string;
  answer: string;
}

export interface LegalContent {
  title: string;
  body: string;
}

export interface ShopFilter {
  id: string;
  label: string;
}
