{
  "name": "functions",
  "engines": {
    "node": "20.11.1",
    "npm": ">=9"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "deploy": "firebase deploy --only functions",
    "serve": "npm run build && cd .. && npx firebase emulators:start --only functions,firestore,storage"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  },
  "devDependencies": {
    "firebase-tools": "^14.11.0",
    "typescript": "^4.9.5"
  }
}
