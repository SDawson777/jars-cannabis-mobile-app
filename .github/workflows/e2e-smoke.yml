name: E2E Smoke Tests (Detox)

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  e2e-smoke:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    env:
      NODE_OPTIONS: '--max-old-space-size=4096'

    steps:
      - name: 🏗 Checkout repository
        uses: actions/checkout@v4

      - name: 🔍 Preflight - Verify lockfiles exist
        run: |
          test -f npm-shrinkwrap.json || (echo "❌ Missing root npm-shrinkwrap.json"; exit 1)
          test -f backend/package-lock.json || (echo "❌ Missing backend/package-lock.json"; exit 1)
          echo "✅ All required lockfiles present"

      - name: 🏗 Setup Node
        uses: actions/setup-node@v4
        with:
          node-version-file: '.nvmrc'
          cache: npm
          cache-dependency-path: |
            npm-shrinkwrap.json
            backend/package-lock.json

      - name: ☕ Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: 🏗 Setup Android SDK
        uses: android-actions/setup-android@v3

      - name: ️ Cache Android SDK
        uses: actions/cache@v4
        with:
          path: |
            ~/.android/avd/*
            ~/.android/adb*
            ${{ env.ANDROID_HOME }}/system-images/*
            ${{ env.ANDROID_HOME }}/platforms/*
            ${{ env.ANDROID_HOME }}/platform-tools/*
            ${{ env.ANDROID_HOME }}/build-tools/*
          key: android-sdk-${{ runner.os }}-api-34
          restore-keys: |
            android-sdk-${{ runner.os }}-

      - name: 📦 Install dependencies
        run: npm ci

      - name: 🔐 Verify Expo token
        run: test -n "${{ secrets.EXPO_TOKEN }}" || (echo "❌ Missing EXPO_TOKEN secret"; exit 1)

      - name: 🏗 Setup Expo & EAS (npm-only)
        uses: expo/expo-github-action@v8
        with:
          expo-version: latest
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
          packager: npm

      - name: 🛠️ Install modern Android cmdline-tools (schema v4 aware)
        run: |
          set -euo pipefail
          # Install latest cmdline-tools using the legacy bootstrap if needed
          "${ANDROID_HOME}/cmdline-tools/bin/sdkmanager" "cmdline-tools;latest" || true

          AVD_NAME="Pixel_XL_API_34"
          # Use the newly installed 'latest' for all subsequent commands

          export PATH="${ANDROID_HOME}/cmdline-tools/latest/bin:${PATH}"

          # Show versions for diagnostics
          sdkmanager --version || true
          avdmanager --help 2>&1 | head -n 1 || true

          # Accept licenses and make sure required packages are present
          yes | sdkmanager --licenses
          sdkmanager --install \
            "platform-tools" \
            "emulator" \
            "platforms;android-34" \
            "system-images;android-34;google_apis;x86_64"

      - name: 🤖 Create / verify AVD (Pixel_XL_API_34)
        env:
          AVD_NAME: Pixel_XL_API_34
          ANDROID_AVD_HOME: ${{ env.HOME }}/.android/avd
        run: |
          set -euo pipefail
          export PATH="${ANDROID_HOME}/cmdline-tools/latest/bin:${PATH}"

          # Clean up any stale AVD entry
          avdmanager delete avd --name "${AVD_NAME}" || true

          # Create with schema-v4 aware avdmanager
          echo "no" | avdmanager create avd \
            --force \
            --name "${AVD_NAME}" \
            --package "system-images;android-34;google_apis;x86_64" \
            --tag "google_apis" \
            --abi "x86_64" \
            --device "pixel_xl"
          # Diagnostics
          ls -la "$HOME/.android/avd"
          grep -n "." "$HOME/.android/avd/${AVD_NAME}.ini"

      - name: 🤖 Start Android emulator
        env:
          AVD_NAME: Pixel_XL_API_34
          ANDROID_AVD_HOME: ${{ env.HOME }}/.android/avd
        run: |
          "${ANDROID_HOME}/emulator/emulator" -avd "${AVD_NAME}" \
            -no-snapshot -no-window -camera-back none -camera-front none \
            -selinux permissive -qemu -m 2048 &
          "${ANDROID_HOME}/platform-tools/adb" wait-for-device shell \
            'while [[ -z $(getprop sys.boot_completed | tr -d "\r") ]]; do sleep 1; done; input keyevent 82'
      - name: 🌐 Expo Web export test
        run: CI=1 npx expo export --platform web || echo "⚠️  Web export not available in this Expo configuration"

      - name: 🏗 Build app for E2E
        run: |
          npm run build:e2e:android
        env:
          EXPO_PUBLIC_API_BASE_URL: http://10.0.2.2:3000/api/v1
          CI: true

      - name: 🚀 Start backend server
        run: |
          npm ci
          npm start &
          # Wait for server to be ready
          timeout 30 bash -c 'until curl -f http://localhost:3000/api/v1/health; do sleep 1; done'
        working-directory: backend
        env:
          NODE_ENV: test
          DATABASE_URL: file:./test.db
          JWT_SECRET: test-secret-key
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY || 'test-key' }}
          FIREBASE_SERVICE_ACCOUNT_BASE64: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_BASE64 || '' }}
          FIREBASE_PROJECT_ID: ${{ secrets.FIREBASE_PROJECT_ID || 'test-project' }}

      - name: 🧪 Run Detox E2E tests
        run: npm run test:e2e:android
        env:
          DETOX_CONFIGURATION: android.emu.debug
          EXPO_PUBLIC_API_BASE_URL: http://10.0.2.2:3000/api/v1

      - name: 📱 Upload test artifacts
        uses: actions/upload-artifact@v4
        if: failure()
        with:
          name: detox-artifacts
          path: |
            artifacts/
            e2e/screenshots/
            e2e/videos/

      - name: 📊 Upload test results
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: e2e-test-results
          path: |
            e2e/test-results/
            coverage/
